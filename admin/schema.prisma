// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource postgresql {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        String    @id @default(cuid())
  admin     Boolean   @default(false)
  name      String    @default("")
  username  String    @unique @default("")
  password  String
  posts     Post[]    @relation("Post_author")
  galleries Gallery[] @relation("Gallery_author")
}

model Post {
  id       String          @id @default(cuid())
  creation DateTime?
  author   User?           @relation("Post_author", fields: [authorId], references: [id])
  authorId String?         @map("author")
  title    String          @default("")
  country  PostCountryType
  content  Json            @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")

  @@index([authorId])
}

model Photo {
  id                  String    @id @default(cuid())
  photo_id            String?
  photo_filesize      Int?
  photo_width         Int?
  photo_height        Int?
  photo_extension     String?
  from_Gallery_images Gallery[] @relation("Gallery_images")
}

model Gallery {
  id       String             @id @default(cuid())
  author   User?              @relation("Gallery_author", fields: [authorId], references: [id])
  authorId String?            @map("author")
  name     String             @default("")
  country  GalleryCountryType
  images   Photo[]            @relation("Gallery_images")

  @@index([authorId])
}

enum PostCountryType {
  latest
  czechia
  lithuania
  romania
  italy
  portugal
  halp
}

enum GalleryCountryType {
  latest
  czechia
  lithuania
  romania
  italy
  portugal
  halp
}
